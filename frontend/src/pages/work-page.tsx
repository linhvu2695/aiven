import { Box, Heading } from "@chakra-ui/react";

export const WorkPage = () => {
    return (
        <Box as="main" p={5}>
            <Heading size="md">Work Management</Heading>
        </Box>
    );
};

export default WorkPage;
